#include <Arduino.h>

TaskHandle_t motorTaskHandle;
TaskHandle_t lcdTaskHandle;
void motorTask(void *pvParameters);
void lcdTask(void *pvParameters);

void setup() {
	xTaskCreatePinnedToCore(motorTask, "MotorTask", 40000, NULL, 1, &motorTaskHandle, 1);
	xTaskCreatePinnedToCore(lcdTask, "LCDTask", 40000, NULL, 1, &lcdTaskHandle, 0);
}

void motorTask(void *pvParameters) {
	while (1) {
		/*
			Motor controll happening here with simpleFOC
			There is access to a sensor that reads the angle of the motor
			I want to send this angle value to the other task
		*/
	}
}

void lcdTask(void *pvParameters) {
    while (1) {
        /*
			This task draws to an LCD screen.
			It uses the motor angle in the drawing code
		*/
    }
}

void loop() {}